# Production – used on the VPS, Gunicorn serves the app
services:
  backend:
    build: .
    container_name: coderr_backend
    ports:
      - '127.0.0.1:8000:8000'
    volumes:
      - ./media:/app/media
    env_file:
      - .env
    restart: unless-stopped
